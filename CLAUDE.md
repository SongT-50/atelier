# atelier

## 이 저장소가 하는 일

> **AI가 스스로를 검증하지 못한다는 문제를, 실제로 속아본 방법으로 푼다.**

여기 있는 것은 도구 자체가 아니라 **그 도구가 지금 살아 있는지 재는 법**이다.
검사가 조용한 것이 「깨끗함」인지 「죽음」인지 못 가르면, 그 검사로 얻은 모든 수치는 무의미하다.

**이 저장소는 공개된다.** 밖에 두고, 남이 쓰고, 반응이 돌아오는 것이 목적이다.

---

## 성공을 무엇으로 판정하나

> ### **우리를 모르는 사람이 여기 있는 것으로 자기 문제를 찾아내고, 그것을 알려온다.**

- **「내려받았다」·「별을 눌렀다」는 성공이 아니다.** 발견은 사용이 아니다.
  실측 근거 — 우리가 공개한 다른 것에서 **발견 20 : 사용 1** 이 관측됐다.
- **날짜로 판정하지 않는다.** *"N개월 안에"* 같은 약속은 아무도 그날 안 본다.
  판정은 **사건**으로 한다 — 위 문장이 일어나는 순간.

### 이 목표가 틀렸다면 무엇이 관측되나 (반증자)

- **오래 조용하면** — 그것은 *"우리가 게을렀다"* 가 아니라
  ### ***"이 방법이 우리한테만 필요한 것이었다"*** 일 수 있다. 그 가능성을 열어둔 채 시작한다.
- **반대로** 사람들이 쓰는데 아무 문제도 안 찾아내면 — 도구가 **아무것도 안 잡고 있는 것**이다.
  그때는 도구가 아니라 **대조군**부터 의심한다.

---

## 여기서 지키는 것 다섯

### 1. 양성 대조군을 도구 **안에** 넣는다

측정 도구를 만들 때, **그 도구가 반드시 잡아야 하는 샘플**을 코드 안에 함께 둔다.
본 검사보다 **먼저** 돌리고, 하나라도 못 잡으면 **그 자리에서 실패시킨다.**

- **왜**: 밖을 재는 일은 산출 대부분이 `0건` 이다.
  그 `0` 이 진짜 부재인지 계측기가 죽은 건지 못 가르면 **모든 수치가 무의미**하다.
- **음성 대조군도 같이 둔다** — 걸리면 안 되는 샘플. 없으면 과잉 검사를 못 본다.
- ### ⚠️ **자기가 만든 문자열로 자기를 시험하면 동어반복이다.**
  샘플이 **검사를 설계할 때 본 그 데이터**에서 나왔으면, 통과는 아무것도 증명하지 않는다.
  ⇒ **실제 대상에 돌려 잡히는지**, 그리고 **정제된 대상에 돌려 조용한지** 둘 다 본다.

### 2. 생성자와 검증자를 나눈다

만든 사람이 자기 것을 평가하면 관대해진다. **밖에 내기 전에는 만들지 않은 쪽이 검사한다.**

검사하는 쪽이 고정할 것: **주장 · 근거 · 분모 · 쓸 수 있는 문장 · 반증자.**

- ⚠️ **검증자의 지적도 확인 대상이다.** 맞힌 실적이 다음 지적의 알리바이가 되면 안 된다.
  고치기 전에 **그 줄을 직접 연다.**

### 3. 수치를 인용하기 직전에 묻는 것

1. **이 숫자는 무엇을 센 것인가** — 파일인가 · 사건인가 · 표본인가.
2. **차이인가 절대값인가** — 대조군을 빼야 할 값을 그대로 쓰고 있지 않나.
3. **어느 조건에서 잰 것인가** — 주장하려는 그 조건에서 잰 게 맞나.
4. **집계값이면 꼬리를 함께 봤나** — `0` 이 *"없음"* 인가 *"상쇄됨"* 인가.
5. **이 이름이 모든 단위에서 같은 것을 가리키나** — 그리고 이 값은 **대상**을 잰 것인가 **관측 조건**을 잰 것인가.
6. **두 값을 나란히 놓기 전에** — 같은 자로 잰 것인가(기간 · 지표 정의 · 모집단 · 이름 체계 · 시간대).

**한 번 잰 값은 근거가 아니다.** 인용될 수치는 **다시 잰다.**
⚠️ 단 **두 번 일치했다고 안정이 증명되지 않는다.** 반복의 목적은 *불안정을 발견하는 것*이다.

**`0` 을 근거로 쓰려면 분모를 함께 적는다.** 관측이 0 이어도 참 비율의 상한은 대략 `3/n` 이다.
`n` 이 작으면 그 `0` 은 **결과가 아니라 「증거 불충분」**이다.

### 4. 규칙을 세울 때 함께 적는 것

새 규칙·게이트·기준을 만들면 그 자리에서 넷을 적는다.

| 칸 | 무엇 |
|---|---|
| **소스 사건** | 어떤 구체적 실패가 이걸 낳았나 |
| **적용 / 비적용** | 어디에 걸고 어디에 안 거나 |
| **비용** | 새로 드는 주의·시간 |
| **반증자** | ### 어떤 관찰이 이 규칙을 **틀렸거나 쓸모없게** 만드나 |

- 반증자에는 **관측 창 · 분모 · 정지 조건**이 있어야 한다. 없으면 그 규칙은 영원히 살아남는다.
- ### **「틀렸다」와 「비싸다」를 섞지 마라.** *"부담이 커서 안 쓰게 된다"* 는 비용이지 반증이 아니다.
  ### 판별 = ***"그 부담이 이 규칙이 이루려던 것을 무너뜨리나?"*** 무너뜨리면 반증이고, 힘들기만 하면 비용이다.
- **절차를 만들 때 한 번 더**: ### *"이걸 열 번 돌리면 열 번 다 같은 답인가? 그러면 왜 돌리나?"*
  매번 같은 답이면 절차가 아니라 형식이다. 반대로 **매번 다른 답이면** 그건 절차가 아니라 그 사람이다.

### 5. 정직 표기

- **안 잰 것은 「안 쟀다」고 적는다.** *"없다"* 와 *"못 찾았다"* 와 *"안 봤다"* 는 다른 문장이다.
- **추측은 추측이라 적는다.** 그러면 확정으로 안 굳는다.
- **검증하지 않은 것을 「검증됨」으로 내보내지 않는다.** 구현됐으나 정확도를 안 쟀으면 그렇게 쓴다.
- **근거를 못 찾은 것은 반대의 근거가 아니다.** 그건 **멈추라**는 신호지 **돌아서라**는 신호가 아니다.
- ### **없는 건 없는 거다.** 잃어버린 측정을 다른 실행값으로 메우지 않는다.

---

## 여기 들어오면 안 되는 것 (경계)

이 저장소는 공개된다. 아래는 **한 조각도 들어오지 않는다.**

- 개발 머신의 **절대 경로** · 사용자명 · 내부 저장소 이름 · 내부 디렉토리 구조
- **비밀** — 키 · 토큰 · 환경변수 파일
- **개인 식별 정보** — 이름 · 연락처 · 계정
- **업무상 비밀** — 거래처 · 정산 · 원장 · 조직 내부 운영 체계
- **사설 IP · 내부 포트 · 내부 호스트명**

### ⚠️ 조심으로는 안 막힌다

새는 길은 본문만이 아니다 — **커밋 메시지 · 예시 데이터 · 테스트 픽스처 · 에러 메시지 · 파일 경로**로 샌다.
⇒ **자동 검사를 통과한 것만 커밋한다.** 그리고 그 검사는 **위 규칙 1** 을 따른다(대조군 없는 검사는 죽은 검사다).

### 이 저장소는 자기 출처를 모른다

여기 있는 규칙들이 **어디서 왔는지 이 저장소는 적지 않는다.**
출처 대응표는 **바깥에** 있고, 그쪽이 이쪽을 가리킨다. **반대 방향은 없다.**
⇒ 그래야 배포물이 **혼자 서고**, 경계가 새지 않는다.

---

## 작업 방식

- **작게 시작하고 하나씩 내보낸다.** 완성하고 내는 게 아니라 내면서 고친다.
- **되돌릴 수 있으면 하고 보고한다.** 되돌릴 수 없거나 밖으로 나가는 것은 **먼저 묻는다.**
- 밖으로 나가는 것 = 발행 · 배포 · 발송 · 공개 저장소 push. **전부 먼저 묻는다.**
